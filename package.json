{
  "name": "quickie-jsx",
  "version": "1.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "eslint src",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "deploy": "surge --project ./build --domain https://keycodes.surge.sh --token $SURGE_TOKEN"
  },
  "keywords": [],
  "author": "Thomas Lebeau <thomas.lebeau@gmail.com>",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thomas-lebeau/keycodes.git"
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-prettier": "^2.10.0",
    "eslint-config-synacor": "^3.0.1",
    "husky": "^0.14.3",
    "if-env": "^1.0.4",
    "node-sass": "^4.11.0",
    "preact-cli": "^2.1.0",
    "sass-loader": "^7.0.1",
    "surge": "^0.20.1"
  },
  "dependencies": {
    "copy-text-to-clipboard": "^1.0.4",
    "decko": "^1.2.0",
    "preact": "^8.4.2"
  }
}
